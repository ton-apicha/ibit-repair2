/**
 * Auth Layout
 * Layout สำหรับหน้า Authentication (Login, Register, etc.)
 */

export default function AuthLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

